# Generated automation-metrics artifacts (machine-local, regenerated by the nightly job).
# The pipeline scripts live in scripts/metrics/ and ARE version-controlled; the data below
# accumulates per-machine and would only create cross-machine churn if committed.
*
!.gitignore
