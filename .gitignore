# Dependencies
node_modules/

# Test results - organized under playwright-report folder
playwright-report/
test-results/
blob-report/
# Intermediate blob output of the /maintain_bundly_automation skill
# (BLOB=blob-report-bundle-$ROOT); the deliverable is playwright-report/Bundle_execution_$ROOT/
blob-report-bundle-*/

# Note: Reports are now versioned with timestamps under playwright-report/
# Each run creates: playwright-report/YYYY-MM-DD-HHMMSS/ (HTML report)
# Each run creates: playwright-report/test-results-YYYY-MM-DDTHHMMSS.json
# Each run creates: playwright-report/junit-results-YYYY-MM-DDTHHMMSS.xml

# Playwright cache
playwright/.cache/

# Allure generated artifacts (regenerated by CI; do not version)
allure-report/
allure-results/

# Lead-Assignment deferred re-verify runtime manifest (written per run; do not version)
deferred-verify/

# Office temp lock files
~$*

# Screenshots and videos
screenshots/
videos/

# Environment variables
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Build output
dist/
build/
*.log

# TypeScript
*.tsbuildinfo

# Temporary files
*.tmp
*.temp

# Config with sensitive data (if needed)
# config/test.config.ts

# Weekly SUBMITTED run plan - regenerated by CRM-Run-Weekly-Submitted on every run
submitted/
